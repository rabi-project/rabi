<!-- SPDX-License-Identifier: Apache-2.0 -->
# IBM calibration harvest — receipt 2026-07-23

Deployment of the hourly-resolution calibration archive onto fleet-0
(harvest-deployment-brief.md). This receipt records counts, checksums, and blob
object names only — **never the calibration data itself** (that lives on the VM
and in Azure Blob; `trace/` stays out of git).

## First pass (hourly backfill, 2026-07-22 → 23)

| Device | Birth | v1 (daily) | v2 (hourly) | On disk |
|---|---|---|---|---|
| ibm_fez | 2024-05-16 | 740 | **12,441** | 6.6 GB |
| ibm_marrakesh | 2024-10-08 | 639 | **11,338** | 6.2 GB |
| ibm_kingston | 2025-03-15 | 466 | **7,518** | 4.3 GB |
| **Total** | | 1,845 | **31,297** | **~17.1 GB** |

- Runtime ≈ 9 h 20 m (fez the pole), ~46,600 probes at ~0.65 req/s/device.
- **0 unexpected errors**; 19 expected pre-birth 404s (silenced by design).
- ~17× the v1 snapshot count — hourly resolution captured the intra-day
  recalibrations the daily sweep missed (fez recalibrates ≈14×/day).
- Secrets-in-logs check: **0** token occurrences in journald.

## First weekly top-up + backup (2026-07-23)

- Top-up: **+33** new snapshots → fez 12,450 / marrakesh 11,352 / kingston 7,528
  (31,330 total). 0 errors.
- Tarball: `trace-20260723.tar.gz`, **1,240,981,959 bytes** (~14:1 gzip).
  - sha256 `610266b5c57dd3b899a06a6d0d5a069b1ccc6fd98d81c89abec3d73b0c9cad48`
- Blob object: `rabi-trace-archive/trace-20260723.tar.gz`
  (account `rabitracearchivekr`, **Standard_GRS**, koreacentral), uploaded via
  the VM's managed identity (no keys on disk).
- **Restore test:** downloaded from Blob → sha256 matches → line counts match
  the live archive (12,450 / 11,352 / 7,528). ✓

## Automation

- `rabi-harvest-weekly.timer`: `OnCalendar=Mon 03:00 UTC` (+≤1 h jitter),
  `Persistent=true`. Next: 2026-07-27. Each run tops up all listed devices
  (watermark-resumable, auto-adopts newly-listed devices), tars, uploads to Blob,
  appends a receipt to `/var/lib/rabi/trace/harvest-log.ndjson`, and keeps the
  newest 8 local tarballs (Blob keeps everything).

## Canonical location

VM `fleet0` (`/var/lib/rabi/trace/`) + Azure Blob `rabi-trace-archive` are now
canonical. The Mac v1 copy is a cold seed and may be archived.
